<div class="top-bottom-margin col-md-12 col-xs-12 text-center">
  <h3><%= @picture.title %></h3>
  <p><%= @picture.artist %></p>
  <%= image_tag @picture.url %>
</div>

<div class="col-md-12 col-xs-12 top-bottom-margin text-center">
  <%= link_to "Back", pictures_url, {:class => "btn btn-default"} %> <%= link_to "Edit", edit_picture_path(@picture), {:class => "btn btn-default"} %> <%= link_to "Delete", @picture, {:confirm => "Are you sure you want to delete this picture?", :method => :delete, :class => "btn btn-danger"} %>
</div>